# PRD — منصة الاختبارات (مدرسة الخيرات للتعليم الأساسي)

## Original Problem Statement
نظام إدارة مدرسي متكامل لمدرسة الخيرات: اختبارات، مشاريع، تقييم سريع، سجلات درجات. مع دعم العام الدراسي (2025/2026 → 2029/2030) وفصلين دراسيين.

## Architecture
- **Backend**: FastAPI + MongoDB (motor) — `/app/backend/server.py`
- **Frontend**: React + TailwindCSS — `/app/frontend/src/`
- **Auth**: JWT
- **LLM**: OpenAI GPT-4o عبر Emergent LLM Key (مولّد بنك الأسئلة)

## Implemented Features

### Core (سابقة)
- مصادقة المعلم + إدارة المدرسين (للمدير)
- اختبارات إلكترونية (MCQ، صح/خطأ، قصير، طويل) + monitor + grading
- مشاريع رقمية وتسليمها
- بنك الأسئلة (مولّد ذكي)
- سجلات الدرجات Grade 5/6 + Grade 7/8 (نموذجان) مع import/export Excel
- بطاقات تقييم (Rubrics) للأنشطة العملية + التقييم من الجوّال

## 2026-09-22 — تبديل الاستيراد إلى فرع conflict_090926_2250
- بطلب المستخدم، أُعيد استيراد المشروع بالكامل من الفرع `conflict_090926_2250` (بدل `main`) عبر `rsync --delete` مع الحفاظ على `backend/.env`/`frontend/.env` المحليين. هذا الفرع يضم إضافات كبيرة غير موجودة في main: نظام حصص الاحتياط الكامل (`/substitution/*`)، جدول الإشراف، وتجديد بصري (خطوط Noto Kufi/Sans Arabic).
- **مشكلة اكتُشفت وأُصلحت**: تسجيل دخول نظام الاحتياط (`ehtiyat`/`ehtiyat2026`) كان يفشل على استيراد جديد لأن `substitution.py::init()` يزرع مستخدم الاحتياط فقط إذا وُجدت متغيرات البيئة `SUB_ADMIN_USERNAME`/`SUB_ADMIN_PASSWORD` — وهي غير موجودة في `.env` الابتدائي. أُضيفت القيمتان + `JWT_SECRET` إلى `backend/.env` وأُعيد تشغيل الخادم، فنجح تسجيل الدخول وأُنشئ `db.sub_users`.
- تحقق وكيل الاختبار: خلفية 47/49 (فشلان بسبب ملف PDF تجريبي غير مرفق بالمستودع `/tmp/supervision.pdf`، ليس خللاً في الكود) وواجهة 100% — `test_reports/iteration_17.json`. تدفق التوزيع الكامل (اختيار غائب → شبكة الحصص → قائمة البدلاء → تكليف) تم التحقق منه فعلياً.
- ملاحظات غير حرجة من المراجعة (لم تُطبَّق، خارج نطاق الاستيراد): `substitution.py` تجاوز 900 سطر ويستحق تقسيماً، واختبارا `test_supervision.py` يحتاجان `pytest.skip` عند غياب ملف PDF التجريبي.

- اختيار العام الدراسي (2025/2026 → 2029/2030) واختيار الفصل (1/2) — لكل معلم تفضيل خاص

### Recent (June 2026 — Import Session)
- ✅ **نقل إرفاق الصور إلى بطاقة التقييم نفسها** (Feb 2026 update):
  - حقل `images` في `rubrics` (حد أقصى 8، base64 data URL ≤ 3MB لكل صورة)
  - واجهة رفع متعدد جديدة في `RubricEditor` (إنشاء/تعديل البطاقة)
  - في `RubricEvaluate`: حُذفت واجهة الرفع لكل طالب، ويُعرض **معرض مرجعي للقراءة فقط** من صور البطاقة، مع معاينة كاملة الشاشة عند النقر
  - تنظيف: حُذف `images` من `RubricEvalSave` ومن مستندات `rubric_evaluations`
- ✅ **استيراد متعدد لسجلات الدرجات + ترميز ألوان حسب الصف**:
  - `Gradebooks.jsx`: استيراد عدة ملفات Excel دفعة واحدة
  - ترتيب: الخامس → السادس → السابع → الثامن → التاسع → العاشر، مع لون مميّز لكل صف
- ✅ **إرفاق صور في تقييم البطاقة** (مُلغى — استُبدل بصور بطاقة البطاقة أعلاه):
  - ~~حقل `images` في `rubric_evaluations`~~ (لم يعد قيد الاستخدام)
- ✅ **تبسيط صفحة الطباعة**: حذف نمط "جدول الصف كامل" + حذف تذييل التوقيع/التاريخ — تبقى صفحة نظيفة فقط بالترويسة والمعايير
- ✅ **طباعة بطاقات التقييم / تنزيل PDF (A4)**:
  - مسار جديد `/teacher/rubrics/:rubricId/print` + زر طابعة على كل بطاقة في `/teacher/rubrics`
  - 3 أنماط: مملوءة لكل طالب (صفحة A4 لكل طالب) / جدول الصف كامل (كشف درجات) / نموذج فارغ للطباعة
  - الترويسة: سلطنة عمان · وزارة التعليم · مدرسة الخيرات للبنين للصفوف ٥-٨
  - العنوان وسط الصفحة + خانتا "الاسم: ..." و"الصف: ..." بخط منقّط
  - جدول بأرقام المعيار / المعيار / الدرجة القصوى / الدرجة + صف المجموع
  - تذييل بخانات (اسم المعلم / التوقيع / التاريخ)
  - `@page A4 portrait` + `@media print` لإخفاء شريط التحكم وضمان A4 صحيح
  - زر "طباعة / حفظ PDF" → window.print() → المستخدم يختار "حفظ كـ PDF" من حوار المتصفح
- ✅ إصلاح بصري لبطاقة تقييم جديدة: خانة المعيار صارت كبيرة (flex-1) وخانة الدرجة صغيرة (5.5rem) باستخدام inline-style لتجاوز `.input-field {width:100%}`
- ✅ تحسين تدفّق "إرسال الدرجات":
  - تأكيد تلقائي عند الانضمام إذا كانت ثقة المطابقة ≥ 0.85
  - تأكيد تلقائي للمطابقات عالية الثقة عند الإرسال (شبكة أمان)
  - عند مطابقة المعلم يدوياً من القائمة المنسدلة → التأكيد تلقائي (لا حاجة لزر إضافي)
  - إزالة نافذة `window.confirm` المتطفّلة — ضغطة واحدة ترسل
  - زر الإرسال يصبح مفعّلاً متى وُجد طالب مطابق (ليس فقط مؤكَّد)
  - شارة الحالة تعرض "N جاهزون للإرسال" بدلاً من "بانتظار المطابقة"
  - relaxed public state endpoint: يكفي matched_student_id (بدون شرط confirmed) لعرض الدرجة

### Recent (Feb 2026)
- ✅ صفحة عرض الدرجات للطالب `/my-grades` (نظام قديم — يدخل الاسم بنفسه)
- ✅ إصلاح z-index للنوافذ المنبثقة عبر React Portal
- ✅ **جلسات إرسال الدرجات** (نظام جديد كامل):
  - المعلم ينشئ جلسة لبطاقة تقييم محددة → يحصل على رابط فريد قصير `/g/{CODE}`
  - يشارك الرابط مع الطلاب → الطالب يكتب اسمه + صفه → ينضم
  - نظام مطابقة تلقائية fuzzy للأسماء + مطابقة يدوية بقائمة منسدلة
  - تأكيد/رفض/حذف لكل مشارك
  - عرض إحصائيات حية (انضموا/بانتظار/مؤكدون/مرفوضون)
  - عند الإرسال: كل طالب يرى **معايير التقييم كاملة** ودرجته على كل معيار + الدرجة الإجمالية + درجة السجل
  - إمكانية إعادة فتح الجلسة لإيقاف العرض

## Key API Endpoints
### Auth/Settings
- `POST /api/auth/login`
- `GET /api/academic-years`, `PUT /api/academic-years/current`, `PUT /api/academic-years/semester`

### Grade Sessions (الجديد)
- `POST /api/grade-sessions` — إنشاء جلسة (rubric_id + gradebook_id)
- `GET /api/grade-sessions` — قائمة جلسات المعلم
- `GET /api/grade-sessions/{sid}` — تفاصيل + قائمة الطلاب في السجل
- `DELETE /api/grade-sessions/{sid}` — حذف
- `POST /api/grade-sessions/{sid}/release` — إرسال
- `POST /api/grade-sessions/{sid}/reopen` — إعادة فتح
- `PUT /api/grade-sessions/{sid}/participants/{pid}` — مطابقة/تأكيد/رفض
- `DELETE /api/grade-sessions/{sid}/participants/{pid}` — إزالة مشارك
- `GET /api/public/grade-sessions/{code}` — معلومات عامة (للطالب)
- `POST /api/public/grade-sessions/{code}/join` — انضمام طالب
- `GET /api/public/grade-sessions/{code}/state?participant_id=...` — استعلام عن الحالة

## Data Models
- `grade_sessions`: `{id, code, owner_id, rubric_id, rubric_title, gradebook_id, grade, section, column, column_label, semester, status (open|released|closed), participants[], released_at, created_at}`
- `participants[]` item: `{id, joined_name, joined_grade, joined_section, matched_student_id, matched_student_name, match_confidence, confirmed, ignored, joined_at}`
- `rubrics`: `{id, owner_id, year, title, semester, column, criteria[], images[], total_max, created_at, updated_at}`
- `rubric_evaluations`: `{rubric_id, gradebook_id, student_id, scores{cid:val}, total, gb_score}`

## Pending / Backlog
- P2: تقسيم `server.py` (~2615 سطر) إلى وحدات routes/models منفصلة
- P2: سجل تاريخي للجلسات المُرسلة (للطالب الذي يريد رؤية درجات قديمة)
- P2: زر ترتيب أبجدي تلقائي لأسماء الطلاب في سجل الدرجات (GradebookDetail)
- P3: رسم بياني radar لمقارنة أداء الطالب بمتوسط الصف

## آخر التحديثات (فبراير 2026 — إصلاح القوائم المنسدلة)
- تصحيح عدد الشعب في `utils.js` (المصدر الموحد): الخامس 7 | السادس 7 (كانت 6) | السابع 6 | الثامن 6 (كانت 5) + دالة `sectionsOfGrade` (fallback للتاسع/العاشر = 7 شعب)
- تحويل حقل "الشعبة" في نافذة إنشاء سجل درجات جديد من نص حر إلى قائمة منسدلة مرتبطة بالصف (`gradebook-section-select`) مع إعادة تعيينها عند تغيير الصف
- توحيد تصميم كل القوائم المنسدلة بنمط `input-field`: فلاتر بنك الأسئلة + نافذة التوليد بالذكاء الاصطناعي + قائمة اختيار الشعبة في صفحة طباعة الروبرك
- إصلاح `frontend/.env` (سطران ملتصقان ENABLE_HEALTH_CHECK/DISABLE_VISUAL_EDITS بعد سحب GitHub)
- بقرار المستخدم: تم الإبقاء على الصفين التاسع والعاشر في قوائم المعلم
- تم التحقق عبر وكيل الاختبار: `/app/test_reports/iteration_2.json` — كل الإصلاحات ناجحة
- إصلاح مظهر القوائم على Safari/iOS (كانت تظهر رمادية بدون نص): فرض `appearance: none` على `select.input-field` + سهم SVG مخصص يسار (RTL) + ألوان الثيم، مع تخصيص للوضع النهاري (كريمي #FBF6E7) — تحقق وكيل الاختبار بنجاح 100% (`iteration_3.json`)
- **إصلاح جذري لمشكلة "الخيارات الفارغة" في القوائم**: أداة visual-edits كانت تحقن `<span>` داخل `<select>` (HTML غير صالح — Safari يُسقط الخيارات). الحل: `DISABLE_VISUAL_EDITS=true` + مسح `node_modules/.cache` وإعادة تشغيل الواجهة. ⚠️ تحذير: لا تُعِد تفعيل visual-edits وإلا ستعود المشكلة. تحقق وكيل الاختبار 100% (`iteration_4.json`)

## آخر التحديثات (فبراير 2026 — ثيم AI لوكس الفاخر)
- ثيم جديد "AI لوكس" (افتراضي عبر ترحيل لمرة واحدة `al_khairat_lux_v1`) مع بقاء نظام تبديل الثيمات (21 ثيم + وضع نهاري)
- خلفية جسيمات مربعة متحركة Canvas 2D (`LuxParticles.jsx`): 3 طبقات عمق، مطر رقمي، موجات، تفاعل ماوس 150px، إضاءة زمردية سينمائية، تخفيض تلقائي للجوال
- لوحة ألوان: أسود #050505 + زجاج rgba(255,255,255,0.03) + زمردي #16D67A/#28F5A7 + خط IBM Plex Sans Arabic/Inter
- أنيميشن دخول متدرج للبطاقات + عدّاد أرقام (CountUp في Dashboard) + توهج جداول عند المرور
- كل أنماط لوكس محصورة بـ `html.theme-lux.in-app` — صفحات الدخول (معلم/طالب) غير متأثرة نهائياً (بطلب المستخدم)
- MatrixBackground يتنحى داخل الموقع لصالح LuxParticles عند تفعيل لوكس
- تحقق وكيل الاختبار بنجاح 100% (`iteration_5.json`): الافتراضي، التبديل، الوضع النهاري، الجداول، الجوال، صفر أخطاء

## آخر التحديثات (فبراير 2026 — بنك أسئلة الكتب: 600 سؤال)
- توليد 600 سؤال (200×3 صفوف: 5، 6، 8 — الفصل الأول) من كتب تقنية المعلومات العمانية الرسمية درساً بدرس عبر Gemini 3 Flash (المفتاح الموحد)
- التصنيف: النوع (اختيار متعدد 4 خيارات/صح وخطأ/قصيرة/طويلة/توصيل pairs)، المستوى المعرفي (تذكر/فهم/تطبيق/استدلال)، الصعوبة (سهل/متوسط/صعب)، الوحدة والدرس
- 98 سؤالاً مرتبطاً بصور حقيقية مستخرجة من الكتب — تُخدم من `/api/static/bank/{grade}/*.jpg`
- سكربت التوليد: `/app/backend/scripts/generate_bank.py` (قابل للاستئناف؛ عند وصول كتاب الصف السابع: أضف مساره وخريطة دروسه في LESSONS ثم `python3 scripts/generate_bank.py 7`)
- خلفية: نقاط `/api/question-bank` أصبحت مقسّمة صفحات {questions,total,page,pages} + `/question-bank/meta` (وحدات/دروس/إحصاءات $facet) + حذف الأسئلة العامة للمدير فقط + create-quiz يستبعد التوصيل ويحمل الصور + توليد عند الطلب عبر Gemini بدل ZAI المعطّل
- واجهة بنك الأسئلة أعيد بناؤها: تبويبات صفوف 5-8، فلاتر (درس بحسب الوحدة/نوع/مستوى معرفي/صعوبة/بحث نصي)، بطاقات ذكية لكل نوع، ترقيم صفحات، إنشاء اختبار من المحدد، الصف السابع حالة فارغة بانتظار الكتاب
- الكتب محفوظة في `/app/data/books/` — تحقق وكيل الاختبار 100% خلفية (16/16) وواجهة (`iteration_6.json`)
- **الصف السابع مكتمل**: 200 سؤال من الكتاب الإلكتروني (https://ict.moe.gov.om/book/PDF/7/cls7_IT_P1) — النصوص من `search_config.js` معكوسة ثم مصححة، محفوظة في `/app/data/books/cls7_pages.json`. الإجمالي الآن **800 سؤال** (بلا صور للسابع — المصدر صفحات مسطحة)

## آخر التحديثات (يونيو 2026 — حذف التوليد بالذكاء الاصطناعي + 260 سؤالاً سهلاً يدوياً)
- **حُذف التوليد بالذكاء الاصطناعي نهائياً بطلب المستخدم** (لتجنب استهلاك رصيد المفتاح الموحد):
  - الخادم: حذف مسار `POST /api/question-bank/generate` + استيراد LlmChat + دوال البرومبت
  - الواجهة: حذف زر ونافذة "توليد بالذكاء الاصطناعي" من بنك الأسئلة (`QuestionBank.jsx`)
  - السكربتات: حذف `generate_bank.py` و`add_easy_questions.py` و`resume_easy.py`
- **إضافة 260 سؤالاً سهلاً مكتوباً يدوياً** (بدون AI، `source: manual_easy`) عبر `/app/backend/scripts/seed_easy_manual.py`:
  - الصف 6: 60 سؤالاً (النمذجة ثلاثية الأبعاد ×3 دروس + الإنترنت + أدوات التواصل + السلامة الرقمية)
  - الصف 7: 100 سؤال (9 دروس كاملة)
  - الصف 8: 100 سؤال (10 دروس كاملة)
- الأرصدة النهائية للأسئلة السهلة: صف 5 = 166 | صف 6 = 171 | صف 7 = 170 | صف 8 = 168 (الإجمالي الكلي 1215 سؤالاً)
- تم التحقق: curl (API يعمل، مسار التوليد يرجع 405) + لقطة شاشة (الزر مختفٍ، الأسئلة السهلة تظهر بالفلاتر)

## آخر التحديثات (أغسطس 2026 — تغيير اسم المدرسة + اسم مدرسة مستقل لكل معلم)
- استبدال "مدرسة الخيرات للتعليم الأساسي" بـ "مدرسة الخيرات للبنين" في كل الموقع (تسجيل دخول المعلم، صفحة انضمام الطالب، الإعدادات الافتراضية، صورة النتيجة، الشهادة)
- ترحيل قاعدة البيانات: كل حسابات المعلمين الحالية (admin, teacher1-3) تحمل الآن `school_name = "مدرسة الخيرات للبنين"`
- **بطاقة الطباعة في التقييم السريع (`RubricPrint.jsx`)**: أصبحت تجلب `school_name` الخاص بالمعلم المسجّل من `/api/auth/profile` وتعرضه في الترويسة — كل معلم (من مدارس أخرى) يرى اسم مدرسته الخاصة عند الطباعة، بدل نص ثابت
- **صورة النتيجة والشهادة**: أصبح `/api/quizzes/{qid}/results` و`/api/quiz/{qid}/result/{sub_id}` يُرجعان `school_name` الخاص بمالك الاختبار، ويُستخدم في `generateResultImage.js` و`generateCertificate.js` بدل النص الثابت
- المعلم يستطيع تعديل اسم مدرسته من الإعدادات (`Settings.jsx`) والمدير من `TeacherManagement.jsx` — تحقق: curl (`/api/auth/profile`) + لقطة شاشة صفحة الدخول

## فحص جاهزية النشر (أغسطس 2026)
- تم تشغيل `deployment_agent` — النتيجة: **PASS**، لا توجد عوائق نشر (لا أسرار مُثبّتة، لا مشاكل CORS/منافذ/بيئة). توجد بعض تحذيرات WARN غير حرجة فقط.

- 2026-09 (إصلاح وضوح): في صفحة أداء الاختبار (StudentExam.jsx) كانت خيارات الأسئلة (اختيار من متعدد/صح-خطأ) وزر "السابق" وزر "إلغاء" في نافذة التأكيد تستخدم لون نص شبه أبيض ثابت `rgba(226,232,240,0.9)` مصمم للخلفية الداكنة فقط — في الثيم النهاري (Tactile OS) كان هذا يجعل النص غير مقروء تقريباً على الخلفية الفاتحة. الإصلاح: قاعدتا CSS جديدتان في index.css (بعد `.text-white` override) تستهدفان محدد `[style*="rgba(226, 232, 240"][style*="rgba(255, 255, 255, 0.0X"]` ضمن `html.mode-light` فقط، فتحوّل النص إلى `#2a2a2a` والحدود/الخلفية لتباين غامق مناسب — الوضع الليلي غير متأثر. تم التحقق عبر إنشاء اختبار تجريبي فعلي وتسجيل دخول كطالب وتصوير الشاشة (خيارات وزر السابق ونافذة التأكيد واضحة تماماً الآن).

- 2026-09 (تنظيم حسب الصف بالألوان): أُضيف حقل `grade` اختياري للاختبارات (لم يكن موجوداً سابقاً؛ التقييم السريع كان يملكه أصلاً) في `QuizCreate`/`QuizUpdate` بالباك-إند + منتقي صف في QuizEditor (تبويب الإعدادات). صفحتا "الاختبارات" (Dashboard.jsx) و"التقييم السريع" (Rubrics.jsx) الآن تجمّعان البطاقات في أقسام حسب الصف بترتيب GRADE_ORDER_NUM مع رأس قسم ملوّن (نفس ألوان `themeOfGrade` المستخدمة في سجل الدرجات) وشريط تلوين على حافة كل بطاقة؛ الاختبارات بلا صف تُجمّع في قسم "بدون صف محدد" بالأخير. تم التحقق عبر إنشاء اختبارات/بطاقات تجريبية لصفوف مختلفة وتصوير الشاشة.

## المهام القادمة (Backlog)
- P1: طباعة ورقة امتحان PDF من بنك الأسئلة (ترويسة الوزارة + نموذج إجابة منفصل)
- P2: زر ترتيب أبجدي تلقائي لأسماء الطلبة في سجل الدرجات

## Credentials
ملف `/app/memory/test_credentials.md` — Admin: `admin` / `teacher123`

## سجل الحوادث (Deployment)
- 2026-09: زر تبديل الثيم للطلاب: مكوّن ThemeModeFab.jsx (زر عائم شمس/قمر أسفل يسار، data-testid="student-theme-toggle") أُضيف إلى StudentJoin وStudentJoinGrade وStudentGrades وStudentResult وStudentLobby وStudentLibrary. التفضيل محفوظ في localStorage (al_khairat_mode) المشترك مع المعلم. + قاعدة CSS لجعل جذور صفحات الطلاب الداكنة المضمّنة فاتحة في mode-light. يتطلب Redeploy.
- 2026-09 (تراجع بطلب المستخدم: "الزر يكون معي انا الذي احدد للطلاب الثيم"): حذف ThemeModeFab.jsx نهائياً وإزالة استيراده/استخدامه من الملفات الستة. الطلاب لا يتحكمون بالثيم إطلاقاً بعد اليوم — يعتمد فقط على الخطّاف الموجود مسبقاً useStudentMode() (يقرأ GET /api/app-settings) الذي يطبّق اختيار المعلم من صفحة الإعدادات (قسم "وضع عرض الطلاب" → PUT /api/app-settings). تم التحقق: تغيير القيمة كمعلم (admin/teacher123) ينعكس فوراً على /my-grades بثيم Tactile OS الفاتح الكامل دون أي زر. صفحتا /student-join و/student-library تحافظان على خلفية سينمائية داكنة ثابتة (قرار تصميم سابق لوضوح النص) بصرف النظر عن الإعداد — سلوك موجود مسبقاً ولم يُغيَّر.
- 2026-09 (استكمال Tactile OS): صفحات الدخول/الانضمام في الوضع النهاري كانت تُبقي فيديو الخلفية الداكن فيختفي العنوان. الحل (نهاية index.css قسم TACTILE 6/7): إخفاء [data-testid="login-video-bg"] في mode-light + خلفية #ECECEC للحاوية عبر :has() + تحييد neon-text والشعار الملون والعنوان الفرعي الأبيض. الفيديو يظهر فقط في الثيم الليلي. يتطلب Redeploy.
- 2026-09: إعادة تصميم الثيم النهاري بالكامل إلى "Tactile OS" (نيومورفيزم أحادي اللون): خلفية #ECECEC، أسطح #F7F7F7، تمييز أسود #111 بنص أبيض، حبوب pill وزوايا 28-32px وظلال ناعمة مزدوجة. التنفيذ: أقسام TACTILE OS في نهاية index.css + توكِنات ThemeContext.jsx + فقاعات رمادية BubblesBackground. التجاوزات المضمّنة inline محصورة بـ html.mode-light.in-app (صفحات المعلم فقط) — ألوان أزرار إجابات الطلاب محفوظة. ملاحظة تقنية: محددات [style*=] يجب أن تطابق الصيغة المسلسلة من المتصفح rgb(x, y, z) بمسافات. الثيم الليلي غير متأثر. اختبار وكيل الاختبار: iteration_7 نجاح 92% ثم أُصلح الزران المتبقيان (bg-yellow-400/bg-teal-600 في محرر الاختبار). يتطلب Redeploy.
- 2026-09: استبدال فيديو خلفية صفحة الدخول /app/frontend/public/login-bg.mp4 بنسخة أعلى جودة (40MB، H.264، moov في البداية للبث التدريجي). يتطلب Redeploy.
- 2026-09: تعديل نموذج 7-10: تقسيم الاختبار القصير (20) إلى اختبارين (10+10). شمل: gradebook.js، GradebookDetail، GradebookSyncButton، Gradebooks.jsx، server.py (GB_FIELDS_78/GB_XL_COLS_78/تصدير واستيراد إكسل/السجل السنوي: منتصف الفصل = (حوار1+عملي1+قصير1)×100÷40 والدرجة النهائية عمود M). ترحيل gb78_quiz_split_v1 يقصّ قيم q1 القديمة >10. الاستيراد يرفض ملفات النموذج القديم برسالة واضحة. يتطلب Redeploy.
- 2026-09: بنك الأسئلة فارغ في الإنتاج (1215 سؤالاً كانت محلية فقط). الحل: تصدير الأسئلة إلى /app/backend/data/question_bank_seed.json + استيراد تلقائي لمرة واحدة في init_db (مفتاح db.meta: question_bank_seed_v1). الصور الثابتة /api/static/bank موجودة أصلاً على اللايف. يتطلب Redeploy.
- 2026-09: كلمة مرور admin في الإنتاج كانت مختلفة + قفل 429. تمت إضافة ترحيل لمرة واحدة في init_db (مفتاح db.meta: admin_pwd_reset_v1) يعيد كلمة المرور إلى teacher123 ويمسح login_attempts. يتطلب Redeploy ليسري على الإنتاج.
- 2026-06: خطأ Cloudflare 520 على كل مسارات /api في النسخة المنشورة (school-frontend-3.emergent.host) رغم عمل المعاينة بشكل سليم ونجاح فحص الجاهزية 3 مرات. التشخيص: مشكلة بنية تحتية (Backend pod متوقف في الإنتاج) وليست مشكلة كود. الحل الموصى به: إعادة النشر (Redeploy) من لوحة Emergent، وإن لم ينجح: إيقاف النشر كلياً ثم النشر من جديد، أو التواصل مع support@emergent.sh.


## آخر التحديثات (يونيو 2026 — نافذة "التحضير" تحت مكتبة الموارد)
- تبويب جديد "التحضير" في مكتبة الموارد → `/teacher/library/plans` (`LessonPlans.jsx` + `LessonPlanSheet.jsx`)، مع رابط التبويب في صفحتي الموارد والفيديوهات
- 40 درساً (الفصل الأول: الخامس 11 | السادس 10 | السابع 9 | الثامن 10) مستخرجة من الكتب المدرسية فقط (`/app/backend/lesson_content.py`: أهداف، مفاهيم بأرقام الصفحات، تدريبات الكتاب، وسائل، تقويم تكويني/ختامي، إثرائي/علاجي، واجب)
- 5 تحاضير لكل درس تُولَّد برمجياً (`/app/backend/lesson_plans.py` — بدون AI) بخمسة أنماط، كل تحضير يضم 6 استراتيجيات مع أرقام الأهداف: (1) الحوار والمناقشة والتعلم التعاوني (2) الاستقصاء والاكتشاف (3) مجموعات الخبراء (4) المهام العملية/المشروع (5) الصف المعكوس والتعلم الذاتي
- نفس قالب المرفق (ترويسة المديرية/المدرسة/العام/المعلم، جدول الصف/الوحدة/الدرس، التعلم القبلي، الأهداف/الاستراتيجيات/آلية التنفيذ/الوسائل، التقويم التكويني/الإثرائي-العلاجي/الختامي/الواجب، ملاحظات المعلم، اعتماد المعلم الأول والمشرف) — بقرار المستخدم حُذف جدول الأيام/الحصص/الشعب
- تعديل وحفظ نسخة المعلم لكل تحضير (`db.lesson_plan_edits` مفتاح owner_id+lesson_id+variant) + استعادة الأصل + تصدير Word (python-docx، A4 أفقي، RTL) عبر `POST /api/lesson-plans/{id}/{variant}/export`
- الترويسة: اسم المدرسة والمعلم من الملف الشخصي والعام من الإعدادات، واسم المديرية حقل جديد `directorate` في `PUT /api/auth/profile`
- API: `GET /api/lesson-plans/catalog`، `GET /api/lesson-plans/{id}`، `PUT/DELETE /api/lesson-plans/{id}/{variant}`
- تحقق وكيل الاختبار 100% خلفية وواجهة (`iteration_8.json`). يتطلب Redeploy.

### تحديث لاحق (يونيو 2026) — استيراد التحضير + نمط الورقة
- زر **استيراد تحضير (Word)** في صفحة التحضير: `POST /api/lesson-plans/import/parse` (multipart .docx) يقرأ جداول القالب (الصف/الوحدة/الدرس، التعلم القبلي، الأهداف، الاستراتيجيات، آلية التنفيذ، الوسائل، التكويني/الإثرائي-العلاجي/الختامي/الواجب، الملاحظات) ويتجاهل جدول الأيام/الحصص، ويطابق اسم الدرس تلقائياً (difflib ≥0.6 ضمن الصف المكتشف) — عند الفشل يطلب من المعلم اختيار الصف والدرس في النافذة (`LessonPlanImport.jsx`)
- يُضاف كتحضير **6، 7، 8...** (`POST /api/lesson-plans/{id}/custom` → `db.lesson_plan_custom`) ويُعدَّل/يُحفظ ويُصدَّر ويُحذف (`PUT/DELETE /{id}/{variant>5}`)
- زر **تبديل نمط الورقة**: "نمط متناسق مع الثيم" (افتراضي — زجاجي داكن ليلاً / نيومورفيزم رمادي نهاراً عبر `.lp-sheet.lp-themed`) ↔ "نمط ورقة Word" الأبيض؛ يُحفظ في `localStorage.lp_sheet_style`
- ألوان قائمة الدروس وتبويبات التحاضير عبر كلاسات (`.lp-nav-item`, `.lp-vtab-*`) مع تجاوزات `mode-light`
- عينة القالب محفوظة في `/app/data/samples/prep_sample.docx`

### تحديث (2 سبتمبر 2026) — إعادة ترتيب الأزرار
- نُقل زر **استيراد تحضير (Word)** ليصبح مجاوراً لزر **تصدير Word** في شريط الأدوات (`LessonPlans.jsx`) بدل موضعه السابق عند تبويبات التحاضير


## 2026-06 — إصلاح نقل درجات بطاقات التقييم السريع
- المشكلة: الدرجة كانت تُحوَّل نسبياً إلى حد الخانة (3/10 → 6/20) فظنّها المعلم جمعاً.
- الحل: إلغاء التحجيم — تُنقل الدرجة كما هي (استبدال) مع سقف حد الخانة. تم تحديث `save_rubric_evaluation` و`_resync_rubric_evaluations_to_gradebooks` في server.py، وتحذير RubricEditor، وإعادة مزامنة التقييمات القديمة.
- نقل درجات الاختبارات (apply-quiz) كان أصلاً استبدالاً — تم التحقق بالاختبار.

## 2026-06 — السؤال القصير يُصحَّح يدوياً
- `submit` في server.py: نوع `short` لم يعد يُصحَّح تلقائياً؛ يُعامل مثل `long` (التسليم يبقى "ينتظر التصحيح").
- GradeAnswers.jsx و QuizResults.jsx: تشمل أسئلة `short` في التصحيح اليدوي، مع عرض الإجابة النموذجية للمقارنة.

## 2026-06 — إصلاح تقطّع فيديو خلفية صفحات الدخول
- السبب الجذري: ملف `/public/login-bg.mp4` كان بحجم **41.6MB** بدقة 2544x1434 وبت-ريت 33Mbps (10 ثوانٍ) — تنزيل ثقيل + فك ترميز مكلف. يضاف إليه فلتر CSS رباعي (hue-rotate/saturate/contrast/brightness) يُطبَّق على كل إطار مع أنيميشن Ken Burns على نفس العنصر.
- الحل:
  - إعادة ترميز إلى `login-bg-720.mp4` (1280x722، CRF 28، بدون صوت، +faststart) = **2.8MB** (تقليل 93%). حُذف الملف الأصلي.
  - `login-bg-poster.jpg` (95KB) كـ poster وخلفية فورية قبل تحميل الفيديو.
  - فصل طبقة Ken Burns (على div حاوية) عن الفلتر (على الفيديو) => المتصفح يخزّن الطبقة المفلترة ويحرّكها فقط.
  - تقليص الفلتر إلى خطوتين (hue-rotate + saturate) — التعتيم يأتي من طبقات التدرّج الموجودة.
  - تقليل جسيمات الغبار من 14 إلى 8.
  - ≤640px: إخفاء الفيديو واستخدام صورة الـposter + إيقاف الأنيميشن. كذلك احترام `prefers-reduced-motion`.
- الملفات: `frontend/src/components/LoginVideoBackground.jsx`، `frontend/src/index.css` (`.login-kenburns`, `.login-video-tinted`, `.login-bg-root`)
- يستفيد منها: TeacherLogin, StudentJoin, StudentLibrary, StudentVideoLibrary, StudentVideoWatch
- ملاحظة: ffmpeg مُثبَّت في البيئة الآن لأي معالجة وسائط مستقبلية.

## 2026-09 — نظام حصص الاحتياط (قسم مستقل بحساب خاص)
- **الدخول**: `/substitution/login` بحساب مستقل (`ehtiyat` / `ehtiyat2026` من `SUB_ADMIN_USERNAME`/`SUB_ADMIN_PASSWORD` في backend/.env) — توكن JWT بدور `substitution` محفوظ في `localStorage.subToken`. روابط الوصول: صفحة دخول المعلم + الشريط الجانبي.
- **البيانات**: 49 معلماً مع الجداول الأسبوعية (الأحد–الخميس × 8 حصص) والأنصبة، مستخرجة من ملف aSc Timetables PDF عبر `backend/substitution_parser.py` (يعالج النص المعكوس + الخلايا المدموجة للحصص المزدوجة) → بذرة `backend/data/substitution_seed.json` تُحمَّل تلقائياً عند أول تشغيل. مجموع الأنصبة 960 = 24 صفاً × 40 حصة (تحقق).
- **التوزيع** (`/substitution`): اختيار التاريخ (الأحد–الخميس فقط) → تحديد أكثر من معلم غائب → شريط 8 حصص (أصفر يحتاج بديلاً / أخضر مكلَّف / فراغ) → قائمة البدلاء المتاحين مرتبة: (أقل احتياط اليوم → أقل احتياط هذا العام → أقل نصاب) مع شارات "أقل نصاب/أقل احتياط/نفس التخصص" + قسم المشغولين. تكليف/إلغاء يدوي، **توزيع تلقائي عادل** بنفس الترتيب مع منع التعارض (لا حصة للبديل في ذات الوقت ولا تكليفين في نفس الحصة).
- **التقرير**: جدول اليوم + طباعة/PDF (`/substitution/print/:date` ورقة A4 بشعار الوزارة، اسم المدرسة، "توزيع الاحتياط ليوم … الموافق …"، أعمدة م/الغائب/الحصة/الصف/المادة/البديل/التوقيع، اعتماد إدارة المدرسة) + تصدير Word (python-docx) + نسخ + واتساب + مسح.
- **المعلمون** (`/substitution/teachers`): بحث/فلتر مادة، تعديل الاسم/المادة/النصاب، تفعيل/تعطيل، حذف، عرض الجدول الأسبوعي، إضافة معلم، **استيراد PDF** (`POST /api/substitution/teachers/import` يحدّث بالاسم ويعطّل غير الموجودين).
- **الإحصائيات** (`/substitution/stats`): فلترة يوم/شهر/عام دراسي (1 سبتمبر → 31 أغسطس)، بطاقات إجمالية، جدول المعلمين (نصاب/غياب/احتياط) مع ترتيب، حسب المادة، سجل الأيام مع رابط طباعة.
- **المجموعات**: `sub_users`, `sub_teachers {id,name,subject,quota,schedule{day:[8]},active,order}`, `sub_days {date,day_name,absent[],assignments[{id,absent_id,period,class,subject,substitute_id,auto}]}`.
- **API** (كلها تحت `/api/substitution`): `auth/login|me|change-password`, `teachers` (GET/POST/PUT/DELETE/import), `day/{d}` (GET/DELETE), `day/{d}/absent` PUT, `day/{d}/candidates` GET, `day/{d}/assign` POST, `day/{d}/auto` POST, `day/{d}/assignments` DELETE, `day/{d}/export` GET (docx), `days`, `stats`.
- الشعار: `frontend/public/moe-logo.jpeg`. الثيم: ورقي فاتح مستقل (`substitution.css` تحت `.sub-root`، ويخفي خلفيات الموقع عبر `html.sub-app`).
- تحقق وكيل الاختبار: خلفية 22/22 وواجهة كاملة (`iteration_10.json`). ملاحظة: pdfplumber أُضيف إلى requirements. يتطلب Redeploy.
- **معلّق من قبل**: تطبيق اسم المنصة "براعة" على واجهة المنصة الرئيسية (لم يُنفَّذ بعد).

### تحديث (2026-09) — نظام الاحتياط: تقرير مجمَّع + استيراد ثلاثي + إعدادات + ثيمات
- **التقرير مجمَّع حسب المعلم الغائب** (ترتيب الغائبين كما أُضيفوا ثم الحصة): صف رأس لكل غائب في جدول الواجهة، rowSpan لاسم الغائب في ورقة الطباعة، دمج خلايا في Word، ونص النسخ/واتساب مجمَّع.
- **الاستيراد** (`POST /api/substitution/teachers/import` — حقول اختيارية `teachers_pdf`، `general_pdf`، `timing`): جدول حصص المعلمين (المصدر الأساسي مع المواد) + الجدول العام (`parse_general_pdf`: الأسماء والأنصبة والجدول بدون مواد — تطابق 100% مع التفصيلي) + التوقيت (صورة/PDF). الدمج بمفتاح اسم مُطبَّع؛ اختلاف النصاب يُرجَع في `quota_mismatch` ويُعتمد العام. نافذة `ImportModal.jsx` بثلاث خانات.
- **الإعدادات** (`/substitution/settings`, `SubSettings.jsx`): تغيير اسم المستخدم/كلمة المرور (`PUT /auth/account` يتطلب كلمة المرور الحالية ويعيد توكن جديد)، اسم المدرسة، أوقات الحصص الثماني (`GET/PUT /settings` → تُستخدم في التقرير والطباعة وWord)، رفع صورة/PDF التوقيت (`POST /settings/timing` — يُخزَّن base64 في `sub_settings` ويُخدم من `GET /settings/timing-file` عام بدون توكن؛ الصورة الافتراضية `backend/data/school_timing.jpeg` تُحمَّل عند أول تشغيل). PDF نصي للتوقيت → محاولة تعرّف تلقائي على 8 حصص مدتها 40 دقيقة (`parse_timing_text`)، وإلا تعديل يدوي.
- **الثيمات**: نهاري (افتراضي) / ليلي / لوكس AI (LuxParticles + زجاج زمردي) — مبدّل في الهيدر وصفحة الدخول والإعدادات، محفوظ في `localStorage.subTheme`، كل الألوان عبر متغيرات `--sub-*` في `substitution.css` (ورقة الطباعة تبقى بيضاء).
- تحقق وكيل الاختبار: 14/14 خلفية جديدة + 22/22 قديمة، الواجهة 100% (`iteration_11.json`). يتطلب Redeploy.

### تحديث (سبتمبر 2026) — نظام الاحتياط: تمييز النصاب العالي + تسمية الملفات + تذكرني
- **تمييز المعلمين ذوي النصاب العالي جداً** في قائمة البدلاء المتاحين (`rank_candidates`): حساب `hq_threshold` = أعلى 20% من أنصبة المعلمين النشطين، وعَلم `high_quota` لكل مرشح ≥ الحد؛ الواجهة (`DistributeParts.jsx`) تعرضهم بخلفية/نقطة كهرمانية + شارة "نصاب عالي جداً" (بدل الأخضر الموحّد)، مع شرح توضيحي (Legend) أعلى القائمة.
- **تسمية ملفات التصدير**: Word/PDF أصبحت بصيغة "احتياط + اسم اليوم + التاريخ" (مثال `احتياط الاثنين 7-9-2026.docx`) عبر ترويسة `Content-Disposition` بترميز UTF-8 (`filename*=`) في `export_docx`، وكذلك في تنزيل Word من الواجهة و`document.title` عند "حفظ كـ PDF" من صفحة الطباعة.
- **تذكرني على هذا الجهاز** (`SubLogin.jsx`): مربع اختيار يحفظ اسم المستخدم/كلمة المرور في `localStorage.subRememberedCreds` (base64) ويعبّئهما تلقائياً في الزيارة التالية؛ إلغاء التحديد يحذف البيانات فوراً.
- تحقق: سكرين شوت آلي (تمييز الألوان + تسمية الملف عبر فحص ترويسة curl + تعبئة تذكرني بعد تسجيل الخروج).

### تحديث (سبتمبر 2026) — نظام الاحتياط: توزيع أعدل وأكثر تنويعاً + كشوفات إحصائية + تصفير
- **قائمة البدلاء المتاحين**: أُضيف `day_periods` (عدد حصص المعلم في نفس اليوم) و`consecutive_alert` (تحذير أحمر "احتياط يومين متتاليين" يظهر فقط في اليوم الثالث ويختفي تلقائياً من اليوم الرابع — منطق `prev_sub_sets(d,3)`: اليوم السابق والذي قبله = True والثالث قبله = False).
- **توزيع تلقائي عادل غير متكرر**: `auto_distribute` يحذف التكليفات التلقائية السابقة (auto=true فقط، يحافظ على اليدوية) قبل إعادة الحساب، و`pick_fair_candidate` يختار عشوائياً من مجموعة صغيرة من المرشحين المتقاربين عدالةً (نفس/قريب من subs_today+subs_year+quota) بدل الأفضل الوحيد دائماً — ضغط الزر مرة ثانية يعطي توزيعاً مختلفاً وعادلاً.
- **كشوفات إحصائية PDF** (`SubStats.jsx` + `SubStatsPrint.jsx` صفحة جديدة `/substitution/stats/print`): وضع "فترة مخصصة" (من/إلى) بجانب يوم/شهر/عام دراسي، فلتر معلم محدد أو "كل المعلمين"، زر "تنزيل كشف PDF" يفتح صفحة طباعة احترافية (جدول عام لكل المعلمين، أو كشف تفصيلي لحصص معلم واحد) مع window.print(). الباك-إند: `GET /stats` يقبل `teacher_id` اختياري ويُرجع `teacher` + `teacher_rows`.
- **منطقة الخطر**: زر "تصفير النظام من البداية" في الإعدادات (`DELETE /system/reset` يمسح `sub_days` فقط، لا يمسّ المعلمين/الإعدادات) وزر "حذف الجميع" في صفحة المعلمين (`DELETE /teachers/all`) — كلاهما يتطلب `window.prompt` بكتابة كلمة تأكيد ("تصفير"/"حذف") قبل التنفيذ.
- تحقق وكيل الاختبار: 6/6 اختبارات باك-إند جديدة + 22/22 قديمة، واجهة 100% (`iteration_12.json`). ملاحظة: `sub_days` أصبحت فارغة بعد اختبار التصفير الفعلي (متوقّع وآمن — المعلمون سليمون 49). يتطلب Redeploy.
- **تحسين مستقبلي مقترح من مراجعة الكود**: تقسيم `substitution.py` (753 سطراً) إلى وحدات فرعية (settings/stats/export) عند أي تعديل كبير قادم.

### تحديث (سبتمبر 2026) — حذف "نفس التخصص" + إضافة "نفس الصف والشعبة"
- حُذف حقل/شارة `same_subject` (نفس التخصص) نهائياً من `rank_candidates` والواجهة واختبارات pytest القديمة.
- أُضيف حقل `same_class`: يتحقق إن كان المعلم البديل يُدرّس نفس الصف/الشعبة (`class`, مثل `7/6`) الذي يُدرّسه المعلم الغائب في تلك الحصة، بالبحث في كامل جدول البديل الأسبوعي (`teaches_class()`). يظهر كشارة كحلية "يُدرّس هذا الصف" (`sub-cand-sameclass-{id}`)، وأصبح معياراً في ترتيب المرشحين (بعد "متاح" وقبل عدد مرات الاحتياط) لتفضيل من يعرف الصف مسبقاً.
- تحقق: فحص مباشر عبر curl (اختفاء same_subject، ظهور same_class بشكل صحيح مع ترتيب صحيح) + سكرين شوت للواجهة يوضح الشارة والترتيب.

### تحديث (سبتمبر 2026) — الأولوية القصوى للأقل نصاباً
- بطلب المستخدم: تغيّر مفتاح الترتيب في `rank_candidates` إلى `(ليس متاحاً، النصاب تصاعدياً، ليس نفس الصف، احتياط اليوم، احتياط العام، الاسم)` — أصبح النصاب الأقل هو المعيار الأهم بعد التوافر مباشرة (كان سابقاً بعد "احتياط اليوم/العام"). كذلك `pick_fair_candidate` (التوزيع التلقائي) يبني مجموعة الاختيار العشوائي حول أقل نصاب أولاً بدل التركيبة الثلاثية القديمة.
- تحقق: فحص مباشر عبر curl يؤكد أن قائمة المرشحين المتاحين مرتبة تصاعدياً بالنصاب تماماً (12، 12، 14، 14... إلخ).

### تحديث (سبتمبر 2026) — جدول الإشراف (Supervision)
- **الاستيراد**: `POST /api/substitution/supervision/import` (PDF جدول الإشراف المدرسي). المُحلّل `parse_supervision_pdf` يقرأ أسماء المشرفين وقادة الإشراف بالاعتماد على مواضع الحروف (يمين→يسار) لمعالجة الحروف المنفصلة وربطات "لا/الله". الأيام تُعيَّن بترتيب الصفوف (أسماء الأيام والعناوين وصف "البوابة" صور داخل الـPDF ولا تُقرأ — وبقرار المستخدم لا يُدرج صف البوابة لأنهم ليسوا مشرفين).
- **المطابقة مع المعلمين** (`match_teacher`): تامة → تسلسل الأسماء (مع حذف "بن" وتطبيع الهمزات/ؤ/ة/ى وضم "عبد"/"أبو") → احتواء. النتيجة على الملف الحالي 42/47 (الـ5 غير المطابقين إداريون غير موجودين في قائمة المعلمين). تُحلّ الأسماء وقت القراءة، فلا تتأثر بتغيّر قائمة المعلمين.
- **الواجهة**: تبويب "جدول الإشراف" داخل صفحة المعلمون (`SubSupervision.jsx`) بجدول مطابق للمرفق (اليوم | قائد الإشراف | المشرفين، قائمة عمودية)، الأسماء غير المطابقة كهرمانية. أزرار: استيراد PDF، تعديل (قائد/إضافة/إزالة مشرف — مثلاً إضافة "ماجد سلطان الشيذاني" المرسوم كصورة)، طباعة (`/substitution/supervision/print`)، مسح. API: `GET/PUT/DELETE /supervision`.
- **التوزيع**: كل مرشح يحمل `supervisor` ('leader'|'member'|null) → شارة حمراء "مشرف اليوم"/"قائد الإشراف اليوم"، ومفتاح "استثناء مشرفي اليوم" (افتراضياً غير مفعّل، محفوظ في `localStorage.subExcludeSup`) يخفيهم في قسم قابل للطي مع زر "تكليف رغم الإشراف"، ويُمرَّر `exclude_supervisors=true` لمسارَي `candidates` و`auto`. صفوف التقرير تحمل `substitute_supervisor` (شارة "مشرف")، وبطاقة الغائبين تعرض قائد إشراف اليوم وعدد المشرفين (`day.supervision`).
- المجموعة: `sub_supervision {id:"main", days:[{day, leader, supervisors[]}], updated_at, source}`.
- تحقق وكيل الاختبار: 7/7 خلفية (`backend/tests/test_supervision.py`) + واجهة كاملة (`iteration_13.json`). يتطلب Redeploy.

### إصلاح (أغسطس 2026) — نقل الدرجات إلى سجل الدرجات: الدرجة الحقيقية بدل النسبة المُحوّلة
- **العلة**: `POST /api/gradebooks/{gid}/apply-quiz` كان يحوّل نسبة الطالب (%) في الاختبار إلى درجة من الحد الأقصى للعمود المستهدف (مثل "الحوار" من 10)، فاختبار من درجة واحدة فقط والطالب حصل على 100% ← تنتقل 10 بدل 1.
- **الإصلاح**: أصبح ينقل `total_score` (الدرجة الفعلية للطالب في الاختبار) مباشرة، مع اقتصاصها تلقائياً عند الحد الأقصى للعمود إن تجاوزته (`min(raw, mx)`) بدل أي تحويل نسبي. يسري على كل أعمدة/نماذج سجل الدرجات وليس فقط "الحوار".
- إعادة النقل لنفس الطالب/العمود من اختبار آخر تستبدل القيمة القديمة تلقائياً (عبر `$set` على المسار المنقّط) — المشكلة المُبلَّغ عنها بأن "الاستبدال لا يعمل" كانت أثراً جانبياً لعلة النسبة نفسها (كل الاختبارات 100% كانت تعطي نفس القيمة القصوى الخاطئة).
- الملفات: `server.py` (`apply_quiz` نحو السطر 1493)، `GradebookSyncButton.jsx` (معاينة الخطوة 2 + نص التلميح).
- تحقق وكيل الاختبار: 4/4 اختبارات باك-إند جديدة (`test_gradebook_sync_scoring.py`) — نقل الدرجة الحقيقية، الاستبدال عند إعادة النقل، الاقتصاص عند تجاوز الحد الأقصى، وعمل الإصلاح على أعمدة متعددة (`iteration_14.json`).

### إصلاح (أغسطس 2026) — تقرير التوزيع: استبدال عمود "التوقيع" بـ "عدد حصص الاحتياط"
- في صفحة طباعة توزيع الاحتياط (`SubPrint.jsx`) وتصدير Word (`export_docx` في `substitution.py`)، حُذف عمود توقيع المعلم البديل الفارغ واستُبدل بعمود يعرض عدد حصص الاحتياط الإجمالية لهذا المعلم البديل **للعام الدراسي كاملاً حتى تاريخ اليوم** (`substitute_subs_year` من `enrich_day`، مبني على `load_counts` بنفس نطاق السنة الدراسية `academic_range`) — وليس فقط عدد حصصه في ذلك اليوم. لم يتأثر مربع "اعتماد إدارة المدرسة" أسفل الصفحة.
- تحقق: سكرين شوت مباشر ليوم 2026-10-25 يعرض قيماً مختلفة (1، 2، 3) تعكس التراكم السنوي الصحيح لكل معلم بديل.

### تحديث (أغسطس 2026) — قائمة البدلاء المتاحين: أيقونة عرض الجدول + عدّاد احتياط الحصة الثامنة
- **أيقونة الجدول**: أُضيفت أيقونة (تقويم) بجانب اسم كل معلم بديل متاح في `CandidateList` (`DistributeParts.jsx`). عند تمرير الفأرة/اللمس عليها يظهر جدول المعلم الأسبوعي مصغّراً أسفل بطاقته مباشرة (نفس تنسيق `sub-week` grid المستخدم في نافذة الجدول بصفحة المعلمين)، ويختفي تلقائياً عند إبعاد الفأرة (`onMouseEnter`/`onMouseLeave`)، مع `onClick` احتياطي للّمس على الأجهزة اللوحية. الباك-إند يُرجع الآن حقل `schedule` الكامل لكل مرشح ضمن `rank_candidates`.
- **عدّاد احتياط الحصة الثامنة**: أُضيف حقل `subs_year_p8` في `rank_candidates` (عبر دالة جديدة `load_period_subs(from,to,period)` تُحصي حصص الاحتياط ضمن حصة معيّنة فقط عبر العام الدراسي)، ويظهر كنص "احتياط الحصة الثامنة: X" في سطر معلومات كل مرشح.
- تحقق: سكرين شوت مباشر (فتح توزيع يوم 2026-10-25، تمرير الفأرة على الأيقونة يعرض الجدول الأسبوعي بشكل صحيح RTL، والعدّاد الجديد ظاهر).
- **تلوين الجدول المصغّر**: حصص المعلم العادية في الجدول المصغّر (`ScheduleMini`) أصبحت بخلفية صفراء (`--sub-amber-soft`)، والخلية المطابقة تماماً لليوم/الحصة الجاريّ توزيعها الآن تُكتب عليها "احتياط" (وليس "حصة الاحتياط") بخلفية خضراء (`--sub-green-soft`) بدل الفراغ "—" — يمرَّر `dayName`/`period` من `SubDistribute.jsx` → `CandidateList` → `ScheduleMini` للمطابقة.

### إصلاح (أغسطس 2026) — الجدول المصغّر لا يظهر بالكامل لبعض المعلمين (خاصة آخر القائمة)
- **العلة**: حاوية قائمة "المعلمون البدلاء المتاحون" كانت تحمل `max-h-[520px] overflow-y-auto` (نطاق تمرير داخلي صغير)، فعند فتح الجدول المصغّر لمعلم قريب من أسفل تلك النافذة الـ520px، كان يُقصّ بصرياً عند حافة الحاوية (خاصة المعلمين ذوي النصاب العالي جداً الذين يظهرون بخلفية صفراء في آخر القائمة عادةً بسبب الترتيب).
- **الإصلاح**: أُزيل تقييد `max-h`/`overflow-y-auto` نهائياً من هذه الحاوية في `DistributeParts.jsx` — البطاقة تكبر الآن طبيعياً مع الصفحة دون أي سياق تمرير متداخل يقصّ المحتوى، لأي معلم في أي موضع بالقائمة.
- إصلاحات إضافية طفيفة اكتشفها وكيل الاختبار: تحويل `<>` إلى `<Fragment key={d}>` في `ScheduleMini` (تحذير React مفتاح مفقود)، وتبسيط منطق زر الأيقونة (الضغط أصبح يفتح الجدول دائماً بدل التبديل toggle) لحل تعارض كان يجعل اللمس على الأجهزة اللوحية يفتح الجدول ثم يغلقه فوراً بسبب تسلسل `onMouseEnter`+`onClick`.
- تحقق وكيل الاختبار: تأكيد الظهور الكامل للجدول (5 أيام × 8 حصص) لأول معلم وللمعلم الأخير رقم 22 (أصفر/نصاب عالي)، وتأكيد سلسلة overflow بالكامل `visible` بلا أي قصّ (`iteration_15.json`). تم تطبيق إصلاحي React key والضغط اللمسي بعد التقرير مباشرة (لم يُعاد الاختبار عليهما تحديداً لكنهما تغييرات بسيطة منخفضة الخطورة تم فحصها بسكرين شوت مباشر).

### تحديث (سبتمبر 2026) — صورة خلفية لصفحة دخول الاحتياط (الوضع النهاري فقط)
- أُضيفت صورة توضيحية (معلمون عمانيون) كخلفية لصفحة `/substitution/login` (`SubLogin.jsx`) في الوضع النهاري (`theme === 'light'`) فقط عبر `style` مباشر (`backgroundImage`/`cover`/`center`) — لا تؤثر على الوضعين الليلي و"لوكس AI". استُبدلت لاحقاً بصورة أوضح بطلب المستخدم؛ الرابط الحالي: `https://customer-assets-39nsmqrw.emergentagent.net/job_school-frontend-3/artifacts/4xt9hn7h_Digen_image_1789729700911.webp`.
- تحقق: سكرين شوت مباشر يوضح ظهور الصورة بوضوح مع بقاء البطاقة والنصوص مقروءة تماماً.

### تحديث كبير (سبتمبر 2026) — تجديد بصري كامل لكل صفحات نظام الاحتياط (من مرجع ui-ux-pro-max)
- بناءً على طلب المستخدم، استُخدم مرجع `ui-ux-pro-max` المحلي لتوليد نظام تصميم (ألوان + خطوط + تأثيرات) لواجهة "لوحة تحكم مدرسية"، وطُبِّق التجديد بالكامل كتغيير CSS فقط في `substitution.css` (بلا أي تعديل على JSX/data-testid/منطق):
  - خطوط جديدة: `Noto Kufi Arabic` للعناوين + `Noto Sans Arabic` للنص (بدل IBM Plex Sans Arabic/Tajawal).
  - لوحة ألوان مُحدَّثة للوضع النهاري (سلايت بارد: خلفية #F1F5F9، كحلي #1E293B، أخضر #16A34A، أحمر #DC2626، تركواز #0D9488) وللوضع الليلي (خلفية #0B111E، بطاقات زجاجية شبه شفافة مع `backdrop-blur`، رمادي مطفي #94A3B8 مطابق لنظام التصميم الموصى به). الوضع "لوكس AI" تُرك كما هو (متوافق أصلاً مع أسلوب Glassmorphism).
  - تأثيرات جديدة: انتقالات `cubic-bezier` أنعم، رفعة عند التمرير (hover-lift) على البطاقات والإحصائيات، حلقة تركيز (focus-visible) على الأزرار والحقول.
  - الثلاثة أوضاع (نهاري/ليلي/لوكس AI) محفوظة كما طلب المستخدم بالضبط.
- تحقق وكيل الاختبار (`iteration_16.json`): 100% نجاح للتدفقات المفحوصة (تسجيل الدخول، تبديل الأوضاع الثلاثة، تدفق التوزيع الكامل، صفحات المعلمون/الإحصائيات/الإعدادات) — بلا أي انحدار وظيفي. ملاحظة بسيطة غير حاجبة عن تباين شارتين شفافتين في الوضع الليلي، عُولجت فوراً بزيادة شفافية الخلفية.

### تحديث (سبتمبر 2026) — مرجع تصميم محلي ui-ux-pro-max
- بطلب المستخدم، حُمِّلت بيانات وسكربتات مهارة `ui-ux-pro-max` (لوحات ألوان لكل قطاع، أزواج خطوط بما فيها عربية RTL، أنماط تصميم) داخل `/app/memory/skills/ui-ux-pro-max/` كمرجع محلي فقط لهذا المشروع — **لم تُسجَّل كـ"Skill" في نظام Emergent** (search_skills/load_skill يعملان فقط مع كتالوج Emergent الداخلي المُعرَّف مسبقاً، ولا يمكن تثبيت مهارة من رابط GitHub خارجي فيه). تُستدعى يدوياً عبر `python3 search.py "..." --design-system` عند الحاجة لأفكار تصميم، إلى جانب `design_agent` الأساسي في المنصة. التفاصيل في `/app/memory/skills/README.md`.

- **ما زال معلّقاً**: تطبيق اسم المنصة "براعة" على واجهة المنصة الرئيسية (P0 — أولوية عاجلة، تم تجاوزها مرتين).
